// The Reveal primitive lives in the motion system; re-exported here so it's
// available from the `@/components/ui` surface alongside the other primitives.
export { Reveal, type RevealProps } from '@/lib/motion';
